name: flutter_first_make
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0-285.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  fluttertoast: ^8.0.9

  ftoast: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
# 이 밑은 들여쓰기가 정말 중요함
# 내가 여기를 비워서 만들었으니 구글링하면서 확인하기
  uses-material-design: true

  assets:
    - assets/


